# Kimi K3 — Thinking-On Benchmark Guide

Exact governed tag: `kimi-k3:cloud`

## Evidence boundary

The exact tag is present in current `ollama list` metadata with digest `630e737485bd`. Prior deliberate benchmark governance identifies Kimi K3 as the required thinking-on quality challenger for the current Ollama Cloud decision. The retained July coding-route attempt received HTTP 402 before any model response; it is availability/quota evidence only and says nothing about Kimi K3 quality.

No Kimi K2.x prompting facts are transferred into this guide. Kimi K3 must not inherit the Kimi K2.6/K2.7 profile by family substring.

## Governed benchmark profile

- API route: Ollama native `/api/chat`, only after separately approved exact-route preflight.
- Thinking lane: top-level `think=true` for every D/R/W/F/T/H/tool-live cell.
- Sampling options: provider defaults. No generation-specific temperature or top-p value is asserted without retained exact-generation guidance.
- Prompt: use the common PA safety/output contract, exact task schema or ordered sections, explicit uncertainty permission, and final-answer-only content.
- Evidence: retain thinking shape/hash separately from final content; do not score hidden reasoning as the answer.
- Validation: completion, exact returned identity, strict format, semantic checks, and critical failures remain separate fail-closed gates.

## Prohibitions

- Do not map `kimi-k3:cloud` to the Kimi K2.6/K2.7 guide.
- Do not treat registration metadata or the previous HTTP 402 as callability or quality proof.
- Do not add a `think=false` lane without freezing a second complete denominator and obtaining approval for the additional 243 calls.
- Do not expose private fixtures or tune frozen validators from candidate output.
